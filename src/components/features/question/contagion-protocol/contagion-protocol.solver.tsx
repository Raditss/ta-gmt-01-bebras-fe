'use client';

import { BaseSolverProps } from '@/components/features/bases/base.solver';

export default function ContagionProtocolSolver({
  questionId
}: BaseSolverProps) {
  console.log(questionId);
  return <></>;
}
